import 'package:flutter/material.dart';

class WritersBenefitSection extends StatelessWidget {
  const WritersBenefitSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
