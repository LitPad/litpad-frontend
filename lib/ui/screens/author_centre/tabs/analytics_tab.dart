import 'package:flutter/material.dart';

class AnakyticsTab extends StatelessWidget {
  const AnakyticsTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
